# AI Assistant (optional — disabled when empty)
AI_PROVIDER=openai
AI_API_KEY=sk-...
AI_MODEL=gpt-4o-mini

# Resend email (optional — invite links shown manually when empty)
RESEND_API_KEY=
RESEND_FROM_EMAIL=Kide CMS <noreply@yourdomain.com>

# Cloudflare Web Analytics (optional — disabled when empty)
CF_BEACON_TOKEN=

# Scheduled publishing auth (optional — open when empty)
CRON_SECRET=
